{
  "name": "scrumws-cloud",
  "version": "0.1.0",
  "private": true,
  "description": "ScrumWS 云端控制面 · P0（身份与连线）+ P1（状态上行）。与仓库根 package.json 完全隔离：根只有 electron/electron-builder 两个 devDep，platform/ 零运行时依赖。",
  "type": "module",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "start": "node src/server.js",
    "bootstrap": "node scripts/bootstrap.mjs"
  },
  "dependencies": {
    "@fastify/static": "^8.3.0",
    "fastify": "^5.2.1",
    "pg": "^8.13.1"
  }
}
